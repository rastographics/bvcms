@model CmsData.Registration.AskSize.Size
@using (var ci = Html.BeginCollectionItem(Model.Name))
{
  <div class="movable well not-wide margin-left-wide">
    @Html.Raw(ci.hiddenindex)
    <div>&nbsp;@Html.Partial("EditorTemplates/QuestionsMenu", new { ConfirmDelete = false })</div>
    @Html.EditorFor(m => m.Description)
    @Html.EditorFor(m => m.SmallGroup)
  </div>
}
