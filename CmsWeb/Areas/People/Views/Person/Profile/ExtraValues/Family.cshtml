@using CmsWeb.Areas.People.Models
@model int
<form class="form ajax span12 margin-top" method="post" data-init="ExtraEditable">
    <fieldset class="left-gutter">
        <legend>Family Extra Values
            <small class="pull-right">
                <a href="/Person2/ExtraValuesStandard/@Model"class="ajax">Standard Values</a> &bull;
                <a href="/Person2/ExtraValuesAdHoc/@Model"class="ajax">AdHoc Values</a>
            </small>
        </legend>
        <table class="table not-wide">
            <thead>
                <tr>
                    <th>Field</th>
                    <th>Value</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var f in StandardExtraValues.GetExtraValues(Model, "Standard").ToList())
                {
                    @Html.EditorFor(m => f)
                }
            </tbody>
        </table>
    </fieldset>
</form>
