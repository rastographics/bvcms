@model CmsWeb.Areas.People.Models.DuplicatesModel
<div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Nick Name</th>
                <th>Middle Name</th>
                <th>Last Name</th>
                <th>Maiden Name</th>
                <th>Birthday</th>
                <th>Email<br />Member Status</th>
                <th>Family Address<br />Personal Address</th>
            </tr>
        </thead>
        <tbody>
            @{
                var dups = Model.person.PossibleDuplicates().ToList();
                var list = Model.person.PossibleDuplicates2().ToList();
            }
            <tr class="info"><td colspan="8"><span style="font-weight:bold">This person</span></td></tr>
            @Html.Partial("System/DuplicateRow", list[0])
            @if (dups.Any())
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">Last and 3 of (first, email, birthday, phone, address)</span></td></tr>
                foreach (var d in Model.person.PossibleDuplicates())
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s1 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">First and partial birthday</span></td></tr>
                foreach (var d in list.Where(d => d.s1 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s2 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">First and birthday</span></td></tr>
                foreach (var d in list.Where(d => d.s2 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s3 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">First and last and no birthday</span></td></tr>
                foreach (var d in list.Where(d => d.s3 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s4 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">First and address</span></td></tr>
                foreach (var d in list.Where(d => d.s4 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s5 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">First and email</span></td></tr>
                foreach (var d in list.Where(d => d.s5 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
            @if (list.Any(d => d.s6 == true))
            {
                <tr class="info"><td colspan="8"><span style="font-weight:bold">Last and birthday</span></td></tr>
                foreach (var d in list.Where(d => d.s6 == true))
                {
                    @Html.Partial("System/DuplicateRow", d)
                }
            }
        </tbody>
    </table>
</div>