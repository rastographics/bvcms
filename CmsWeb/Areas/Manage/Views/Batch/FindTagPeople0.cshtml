@using CmsWeb
@{
    ViewBag.Title = "Find Tag People";
    ViewBag.PageHeader = "Find Tag People";
    Layout = ViewExtensions2.TouchPointLayout();
}
<div class="row">
    <div class="col-md-9">
        <div class="box box-responsive">
            <div class="box-content">
                <div class="alert alert-info">
                  One row each, tab separated columns (copy from Excel)
                  with First, Last, Birthday, Email, Phone, Phone2, Phone3 
                  (be sure to use a header row)
                </div>
                <form action="/Batch/FindTagPeople" method="post">
                    <div class="row">
                        <div class="col-sm-5">
                            <div class="form-group">
                                <label for="tagname">Tag Name</label>
                                @Html.TextBox("tagname", "", new { @class = "form-control" })
                            </div>
                        </div>
                    </div>
                    @Html.TextArea("text", "", new { rows = "20", cols = "100", wrap = "off", @class = "form-control" })<br />
                    <input type="submit" class="btn btn-primary" value="Submit" />
                </form>
            </div>
        </div>
    </div>
</div>