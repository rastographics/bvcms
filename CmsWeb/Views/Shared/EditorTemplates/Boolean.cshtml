@model bool
<div class="control-group">
    @if (((string) ViewBag.label).HasValue())
    {
        @Html.LabelFor(m => m, ((string)ViewBag.label), new {@class = "control-label"})
    }
    else
    {
        @Html.LabelFor(m => m, new {@class = "control-label"})
    }
    <div class="controls">
        <p>@Html.CheckBoxFor2(m => m, true)</p>
    </div>
</div>
