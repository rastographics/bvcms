@model string
@{
}
<div class="control-group @ViewBag.hide">
    @Html.LabelFor(m => m, new { @class = "control-label"})
    <div class="controls">
        @Html.TextAreaFor(m => m, 5, 80, null)<span class="help-block">@Html.Raw(ViewData["help"])</span>
    </div>
</div>
