@model string
<div class="form-group @ViewBag.hide">
    @Html.LabelFor(m => m, new { @class = "control-label"})
    <div class="controls">
        @Html.TextAreaFor(m => m, 5, 80, new { @class = "form-control "})
        <span class="help-block">@Html.Raw(ViewBag.help)</span>
    </div>
</div>