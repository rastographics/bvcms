@model CmsWeb.Code.CodeInfo
<div class="control-group">
    @Html.LabelFor(m => m, new { @class = "control-label" })
    <div class="controls">
        @if (Model != null)
        {
            <p>@Model</p>
        }
        else
        {
            <p>&nbsp;</p>
        }
    </div>
</div>
