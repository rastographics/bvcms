@using UtilityExtensions
@model decimal?
<dl>
    @if (ViewBag.label != null)
    {
        <dt>@ViewBag.label</dt>
    }
    else
    {
        <dt>@Html.DisplayNameFor(m => m)</dt>
    }
    <dd>@Model.ToString2("N2")</dd>
</dl>
