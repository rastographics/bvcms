<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="hidden-lg hidden-xs">Orgs</span><span class="visible-lg-inline visible-xs-inline">Organizations</span> <span class="caret"></span></a>
    <ul class="dropdown-menu">
        @Helper.LiAnchorLink("Search / Manage", "/OrgSearch/")
        @if (User.IsInRole("Edit"))
        {
            <li>@Helper.JQueryIdLink("Add New Organization", "addorg")</li>
            @Helper.LiAnchorLink("Manage Org Members", "/OrgMembers/")
        }
        @Helper.LiAnchorLink("Coupons", "/Coupons/", User.IsInRole("Coupon"))
        @Helper.LiAnchorLink("Manage Promotion", "/Promotion/", User.IsInRole("Edit"))
        @{
            var recentOrgs = Util2.MostRecentOrgs;
        }
        @if (recentOrgs.Any())
        {
            <li class="divider"></li>
            <li class="dropdown-header">Recent</li>
            foreach (var o in Util2.MostRecentOrgs)
            {
                @Helper.LiAnchorLink(o.Name, "/Organization/" + o.Id)
            }
        }        
    </ul>
</li>
